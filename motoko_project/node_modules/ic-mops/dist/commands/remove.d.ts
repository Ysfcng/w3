type RemoveOptions = {
    verbose?: boolean;
    dev?: boolean;
    dryRun?: boolean;
    lock?: 'update' | 'ignore';
};
export declare function remove(name: string, { dev, verbose, dryRun, lock }?: RemoveOptions): Promise<void>;
export {};
