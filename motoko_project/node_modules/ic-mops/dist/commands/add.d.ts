type AddOptions = {
    verbose?: boolean;
    dev?: boolean;
    lock?: 'update' | 'ignore';
};
export declare function add(name: string, { verbose, dev, lock }?: AddOptions, asName?: string): Promise<void>;
export {};
